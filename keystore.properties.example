# Copy to keystore.properties (gitignored) at repo root for command-line signed FOSS release builds.
# Paths are relative to the repository root unless you use an absolute path for storeFile.
#
# storeFile=path/to/your-release.keystore
# storePassword=...
# keyAlias=key0
# keyPassword=...
